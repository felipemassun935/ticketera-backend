name: Deploy Backend

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy backend
        run: |
          cd /opt/backends/ticketera-backend
          git pull origin main
          docker compose up -d --build
          docker image prune -f